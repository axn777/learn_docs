

# TODO

配置好管线，关于SCAIL 单人 和 双人 的

如何生成到blender中呢、

+ 如果实现模型，拍摄的时候，全身，跟踪动作，然后在blender中，修正一下镜头跟随策略呢？
+ 可能选哟

尝试用 wan animate 修正表情，用scail处理肢体动作

+ 对比 wan 和 scail 的动作迁移，找下区别

看一下源代码中的各种适配管线

跟 Wan2 配合的各种模型，lora，vae

试一下用 Wan2.2 模型跑

为什么量化不能用 fp8 呢

开源 Ace-step1.5 用法文档：https://fcnv0krxbr5n.feishu.cn/wiki/F4xbwytRiirAiskXxi2ccNWWnxb?from=from_copylink

# 2_13

### 先看动作检测的代码

### Gradio 快速构建机器学习

先看Wan2.2模型



# 2_12

### 量化库会裁剪

+ umt5-xxl-enc-fp8_e4m3fn 维度 768
+ umt5_xxl_fp16 维度 4096
+ ??? 检查维度的代码不管用

### Vace

+ [VACE](https://ali-vilab.github.io/VACE-Page/)

+ VACE creatively provides solutions for video generation and editing within a single model, allowing users to explore diverse possibilities and streamline their workflows effectively, offering capabilities including **Move-Anything**, **Swap-Anything**, **Reference-Anything**, **Expand-Anything**, **Animate-Anything** and more.

# 2_11

### 先看下 Transforms 库

+ hf下载遇到网速的问题，不知道如何解决，先手动下载了，难道跟windows有关 ???
  + 手动下载了，不管了

### FLux

+ 试一下就好了，不管了

### Wan源码看一下

+ Uni3C

### Scail 源码

# 2_10

先系统走一圈，ComfyUI的基础知识

### SD

SD 的例子，

OpenPose 卡内基梅隆大学

hires.fix 放大，两次采样

Lora

# 2_9

### Hires.fix 放大

1. **先生成低分辨率 latent**：

从 `Upscale Latent` 的输入 `samples` 可以看出，前面一定有一个 KSampler 生成了 512×512 或 512×768 的 latent。这对应了 Hires.fix 的 “先生小图”。

2. **Latent 放大到 1536×1536**：

`Upscale Latent` 节点把小 latent 放大到了高清尺寸。这对应了 Hires.fix 的 “再放大”。

3. **用 denoise=0.5 进行重采样**：

第二个 KSampler 接收了放大后的 latent，并设置了 denoise=0.5。这对应了 Hires.fix 的 “最后精修”。

# 2_8

### 解决的渲染问题

 1. cfg 需要为 1 否则画面非常不好
  2. 需要WanVideoContextOptions, 来提供上下文,否则动作被锁定了
  3. 有时候识别人会丢失,这里需要处理下,看看哪个近用哪个吧
  4. 240 问题,高度需要卡死为 120 倍数,还需要32倍数


# 2_6

### 跑通最简模型，去掉不必要的节点

### 问题： **WanVideoSamplerFromSettings**

+  Sizes of tensors must match except in dimension 1. Expected size 300 but got size 240 for tensor number 1 in the list.

+ 这个 300 是 WanVideo 2.2 版本的核心要求（2.1 版本是 240）

  ​	Using SCAIL embeddings:  ref_latent_pos: torch.Size([20, 1, 300, 224])

  ​	单帧的特征总量 300×224=67200    300个上下文 * 每个上下文224维度

```
log.info("Using SCAIL embeddings:")
for k, v in scail_embeds.items():
    log.info(f"  {k}: {v.shape if isinstance(v, torch.Tensor) else v}")
```

+ 看一下主流的插件

  + 

+ 修一下主板，大概6到7点钟到

  + 主板已经确定延迟3天了


# 2_5

+ 跑通 Scail 插件全流程
  + huggingFace 下载模型
  
    ```bash
    hf download --token hf_xxxxx onnx-community/yolov10m
    ```
    
  + 模型和工作流分离出来
  
    + detection 加载
  
      + onnx 需要指定 detection 目录，在ComfyUI里面，不能放到 Extra_Models
  
    + 工作流（管理员 Cmd 创建）
  
      ```bat
      # user/default/workflows
      mklink /D workflows "D:\Works\ComfyRes\Workflows" 
      # input
      mklink /D input "D:\Works\ComfyRes\Input" 
      # output 
      mklink /D output "D:\Works\ComfyRes\Output" 
      # models
      mklink /D models "D:\Works\ComfyRes\Models" 
      ```
  
  + 使用PyCharm启动服务器
  
    + 关闭异常（杀死进程）
  
      ```
      netstat -ano|findstr 8188
      tasklist | findstr 20600
      taskkill /f /t /pid 20600
      ```
  
+ Scail 输入参数，视频如何输入进去训练的，只是传入dwpose视频么

  + [**ComfyUI-FlashVSR_Ultra_Fast**](https://github.com/lihaoyun6/ComfyUI-FlashVSR_Ultra_Fast)  vs ComfyUI-FlashVSR_Stable 先选择 Fast 的
  + [**ComfyUI_essentials**](https://github.com/cubiq/ComfyUI_essentials) 一些核心组件
  + [**comfyui_controlnet_aux**](https://github.com/Fannovel16/comfyui_controlnet_aux) 辅助的controlnet
  + [**ComfyUI-SDPose-OOD**](https://github.com/judian17/ComfyUI-SDPose-OOD)  xxxx
  + [**ComfyLiterals**](https://github.com/M1kep/ComfyLiterals)
  + [**Comfyroll Studio**](https://github.com/Suzie1/ComfyUI_Comfyroll_CustomNodes)

+ 看下 Scail 全插件的知识

  + 跑通了模型，但是有些插件是用的比较旧的，更新迭代一下

+ 试验下性能，本地5090能力，以及量化版本的能力

  + Warming up NLF model... 这一步很慢


# 2_4

### 安装机器
+ 管理员权限

	UAC，需要设置，大概第二档，不能总是管理员权限
+ 用户权限

		如果装在C盘，不属于任何用户
		如果非C盘，属于当前用户，这样其他用户会有麻烦
	
	​		比如conda，env 目录，其他用户无法写入 
	
	```bat
	# 查看当前的 env dirs
	conda config --show envs_dirs
	```

### CompyUI 插件安装等

+ Scail 插件安装
  + [**ComfyUI-Crystools**](https://github.com/crystian/ComfyUI-Crystools) 性能
  + [**ComfyUI-KJNodes**](https://github.com/kijai/ComfyUI-KJNodes) KJ Nodes
  + [**ComfyUI-WanVideoWrapper**](https://github.com/kijai/ComfyUI-WanVideoWrapper) 
  + [**ComfyUI-WanAnimatePreprocess**](https://github.com/kijai/ComfyUI-WanAnimatePreprocess)
  + [**ComfyUI-VideoHelperSuite**](https://github.com/Kosinkadink/ComfyUI-VideoHelperSuite)

### 碰面对计划

+ 阶段1
  + 先熟悉ComfyUI
  + 熟悉各种大模型调用
  + 熟练用Scail，以及内部实现的原理
  + 试出来效果是否可行
+ 阶段2
  + 云端跑通模型
  + 批量做剧情，解决遇到的问题
  + 出一些简单的容易上手的简易工作流，避免操作复杂，并行开发


# 2_3

### 安装 CompyUI

+ 网页安装

+ UIManager冲突解决

+ 跑例子，ComfyUI-SCAIL-Pose -- Kijai

  + vitpose 模型

    + ONNX Detection Model Loader -- 加载 onnx 格式（更开放的格式）



    +	yolov10m.onnx（You Only Look Once）: 识别图像中各种目标框，类别
    +	vitpose-l-wholebody.onnx：人体姿态估计模型

  + Pose Detection VitPose to DWPose


    +	DWPose（133点）：23（身体核心） + 42（双手） + 34（双脚） + 34（面部）
    
      +	也就是 COCO-WholeBody 133 点
    
    +	vitpose-l-wholebody：也是CoCo 133点

  + Render NLF Poses

    + NLF（神经定位场）

      3D 人体 24 点姿态估计，但是目前 NLF 没有面部3d的能力，如果面部能力，**需要配合其他**

      （3DDFA、FLAME）

    + Taichi（太极，清华计算所）

      3D并行后处理计算，归一，插值，无效筛选，3D空间对齐，并行处理帧

### Hugging Face 下载器

 + download Token

   ```bat
      pip install huggingface_hub[cli]
   ```
   
+ huggingface-cli 

  huggingface-cli download bert-base-uncased --local-dir <huggingface_models/bert-base-uncased>





### 问题

+ ComfyUIManager中遇到很多冲突，比如 KJNodes 中跟 CompyUI_Swwan
