## bitsandbytes 8bit 量化

安装量化 bitsandbytes 

```bat
# 下载 transformers
pip install transformers --pre

# 核心依赖（bitsandbytes 支持 CUDA 量化） 
pip install bitsandbytes transformers accelerate torch safetensors -i https://pypi.tuna.tsinghua.edu.cn/simple
```

### 量化运行代码（8bit）

```python
import torch
from transformers import AutoModelForCausalLM, AutoTokenizer

# 模型路径/名称（替换为你的 Wan2.1-14B-SCAIL.safetensors 所在路径）
model_path = "./Wan2.1-14B-SCAIL.safetensors"
# 加载 tokenizer（需匹配 Wan2.1 模型的分词器）
tokenizer = AutoTokenizer.from_pretrained(model_path, trust_remote_code=True)

# 加载模型并做 8bit 量化
model = AutoModelForCausalLM.from_pretrained(
    model_path,
    device_map="auto",  # 自动分配模型到显存/内存（显存不足时自动卸载到内存）
    load_in_8bit=True,  # 开启 8bit 量化
    torch_dtype=torch.float16,  # 基础精度
    trust_remote_code=True,
    safetensors=True  # 明确加载 safetensors 格式
)

# 推理示例
prompt = "你好，请介绍一下自己"
inputs = tokenizer(prompt, return_tensors="pt").to("cuda")
outputs = model.generate(
    **inputs,
    max_new_tokens=100,  # 生成最大长度
    temperature=0.7,     # 生成随机性
    do_sample=True
)
print(tokenizer.decode(outputs[0], skip_special_tokens=True))
```
### 量化运行代码（4bit）

```


model = AutoModelForCausalLM.from_pretrained(
    model_path,
    device_map="auto",
    load_in_4bit=True,  # 开启 4bit 量化
    torch_dtype=torch.float16,
    trust_remote_code=True,
    safetensors=True,
    # 4bit 量化优化配置（必加）
    bnb_4bit_quant_type="nf4",  # 针对自然语言的 4bit 格式
    bnb_4bit_compute_dtype=torch.float16,  # 计算时的精度
    bnb_4bit_use_double_quant=True,  # 双重量化，进一步降低显存
)

# 推理逻辑和 8bit 完全一致
prompt = "你好，请介绍一下自己"
inputs = tokenizer(prompt, return_tensors="pt").to("cuda")
outputs = model.generate(**inputs, max_new_tokens=100)
print(tokenizer.decode(outputs[0], skip_special_tokens=True))
```



## GPTQ 8bit 离线量化

pip install auto-gptq transformers datasets -i https://pypi.tuna.tsinghua.edu.cn/simple